1. Using Contains will allow you to check ifr a string contains a specfic char within the string.

2. Code

 class Solution
    {
        static void Main(string[] args)
        {
            string[] catNames = { "Lucky", "Lucky", "Bella", "Kermit", "Toad" };
            
            for (int i = 0; i < catNames.Length; i++)
            {
                if (catNames[i].Contains('a'))
                {
                    Console.WriteLine(catNames[i]);
                }
            }
            Console.ReadLine();
        }
