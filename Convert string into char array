 public void InterpretExression(string expression)
        {
            char[] expressionArray = expression.ToCharArray();
            foreach (var e in expressionArray)
            {
                Console.WriteLine(e);
            }
        }
